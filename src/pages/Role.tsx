import { Stack } from "@mantine/core";
import AddRoleModalButton from "../features/roles/AddRoleModalButton";

const Role = () => {
  return (
    <Stack gap={"xl"}>
      <AddRoleModalButton />
    </Stack>
  );
};

export default Role;
